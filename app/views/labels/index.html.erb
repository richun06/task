<h2>ラベル一覧</h2>
<table>
  <thead>
    <tr>
      <th><%= Label.human_attribute_name(:name) %></th>
    </tr>
  </thead>
  <tbody>
    <% @labels.each do |label| %>
      <tr>
        <td><%= label.name %></td>
        <td><%= link_to "編集",edit_label_path(label.id) %></td>
        <td><%= link_to "削除",label_path(label.id), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>